sudo: required

services:
  - docker

script:
  - docker run -it -v "$(pwd)":/usr/app -w /usr/app r3ddox/node-pdftk npm install
  - docker run -it -v "$(pwd)":/usr/app -w /usr/app r3ddox/node-pdftk npm test